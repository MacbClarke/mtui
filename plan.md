# 动态 SSH 隧道 TUI 工具开发设计框架

---

## 1. 核心设计原则

* **零远端依赖**：仅依赖目标机现有的标准 OpenSSH 服务（`sshd`），无需在远端安装额外程序或 Agent。
* **单连接多路复用**：全程维持一条 SSH 物理长连接，所有端口转发均基于 SSH 标准协议中的 `direct-tcpip` 逻辑通道按需启闭。
* **前后台解耦**：终端 UI 线程与网络 I/O 完全独立，通过异步通道（Channel）收发指令，保证界面操作不卡顿。

---

## 2. 架构概览

```text
[ Ratatui TUI 界面 ]
        │  ▲
        │  │ (通过 mpsc::channel 传递控制指令与状态回报)
        ▼  │
[ 隧道控制中心 (Tunnel Manager) ]
        │
        ├──【本地端口监听池 (tokio::net::TcpListener)】
        │     - 每个映射规则对应一个独立的监听协程
        │     - 通过 watch/oneshot 信号实现动态停用并释放端口
        │
        └──【SSH 会话管理 (russh::client::Handle)】
              - 维护与远端服务器的单一物理 TCP 会话
              - 本地监听到流量时，动态开辟 direct-tcpip 信道
              - 双向透明中继本地套接字与远端信道流量
```

---

## 3. 技术选型

* **异步引擎**：`tokio`（任务调度、异步网络 I/O）
* **SSH 协议栈**：`russh`（纯 Rust 异步客户端实现，原生支持 `direct-tcpip`）
* **终端渲染**：`ratatui` + `crossterm`（轻量级 TUI 界面绘制与键盘事件驱动）
* **配置解析**：`ssh2-config`（可选，直接读取系统 `~/.ssh/config` 配置）

---

## 4. 核心工作流

1. **会话建立**：启动时加载配置和私钥，与目标服务器建立一条主 SSH 会话。
2. **新增隧道**：
   * 用户在 TUI 输入本地端口与远端目标（如 `8080 -> 127.0.0.1:80`）。
   * 控制中心启动本地 `TcpListener` 监听。
   * 有客户端连入时，向已建立的 SSH 会话申请一条 `direct-tcpip` 虚拟信道，并在两者之间双向中继数据。
3. **销毁隧道**：用户在 TUI 触发关闭，控制中心发送关闭信号，注销对应的监听协程并释放本地端口。
4. **状态同步**：后台统计各隧道的存活状态和吞吐流量，定期推送到前台表格刷新渲染。

---

## 5. 建议迭代路线

* **Step 1：验证核心链路（纯 CLI）**  
  不写 UI，仅用代码验证：使用 `russh` 连通服务器，并成功将本地某端口流量通过 `direct-tcpip` 转发到远程目标。
* **Step 2：动态生命周期管理**  
  封装 `TunnelManager`，验证在不中断 SSH 物理连接的前提下，随时新增、取消本地端口监听。
* **Step 3：TUI 基础交互**  
  使用 Ratatui 搭建主列表视图，支持通过键盘快捷键触发新增、删除操作。
* **Step 4：体验增强**  
  支持读取本机 `~/.ssh/config` 主机别名、增加实时上下行速率统计、补充连接断开自动重连机制。
